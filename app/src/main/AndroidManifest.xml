<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hotmartapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ui.main.MyApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_flame"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_flame"
        android:supportsRtl="true"
        android:theme="@style/Theme.HotmartApp">
        <activity
            android:name=".ui.details.DetailsActivity"
            android:theme="@style/Theme.HotmartAppNoActionBar"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>